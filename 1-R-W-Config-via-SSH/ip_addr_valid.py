import sys

def ip_addr_valid(ip_list):

    for ip in ip_list:
        ip = ip.rstrip("\n")
        octets = ip.split(".")
        # print(octets)

    if (len)

ip_addr_valid(["10.10.10.2\n", "10.10.10.3\n"])